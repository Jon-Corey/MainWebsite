{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "start http://localhost:8080 && npm run start",
            "name": "Run Eleventy",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}